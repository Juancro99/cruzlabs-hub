# Cruzlabs Hub

Static hub for Cruzlabs apps hosted on Coolify.

## Local

Open `index.html` directly or build the container:

```powershell
docker build -t cruzlabs-hub .
docker run --rm -p 8080:80 cruzlabs-hub
```
